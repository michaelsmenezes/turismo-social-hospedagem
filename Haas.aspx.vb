Imports System.Drawing

Partial Class Haas
    Inherits System.Web.UI.Page


    Private Sub Page_Load(ByVal sender As System.Object, ByVal e As System.EventArgs) Handles MyBase.Load
        'Dim imagem1 As New Bitmap(468, 60)
        'Dim g As Graphics = Graphics.FromImage(imagem1)
        'g.FillRectangle(Brushes.Red, 0, 0, 468, 60)
        'g.DrawRectangle(Pens.Black, 0, 0, 467, 59)
        'Dim font As New Font("Rockwell Extra Bold", 20, FontStyle.Regular)
        'g.DrawString("TESTE Images", font, Brushes.DarkSlateBlue, 16, 16)
        'Dim Icone As Image
        'Icone = Image.FromFile(Server.MapPath("images/AZ.png"))
        'g.DrawImageUnscaled(Icone, 295, 7)
        'imagem1.Save(Response.OutputStream, Imaging.ImageFormat.Gif)
        'g.Dispose()
        'imagem1.Dispose()
    End Sub

End Class
